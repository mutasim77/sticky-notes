import MainContainer from "@/app/components/MainContainer";

const Timer = () => {
    return (
        <MainContainer keywords="notes page, notes, sticky notes" title=" Timer">
            <h1 className="text-center">Timer Page</h1>
        </MainContainer>
    )
}

export default Timer;
