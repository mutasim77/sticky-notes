export interface INotes {
    id: string,
    date: string,
    title: string,
    note: string,
    color: string
}

export interface ICategories {
    id: string,
    title: string,
    color: string,
}